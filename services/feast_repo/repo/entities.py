from feast import Entity

user = Entity(
    name="user",
    join_keys=["user_id"],
    description="Utilisateur StreamFlow identifié de manière unique par user_id",
)
from feast import Entity

user = Entity(
    name="user",
    join_keys=["user_id"],
    description="Utilisateur StreamFlow identifié de manière unique par user_id",
)
